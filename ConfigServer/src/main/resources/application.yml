server:
  port: ${PORT:8888}

spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AlejaMarin/EF-Config
          force-pull: true